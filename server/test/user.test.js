
import assert from 'assert';

describe('User API', function() {
  it('should return 2 + 2 = 4', function() {
    assert.strictEqual(2 + 2, 4);
  });

  it('should check that true is not false', function() {
    assert.notStrictEqual(true, false);
  });
});
